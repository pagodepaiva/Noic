#include<bits/stdc++.h>
using namespace std;
int main()
{
    cin.tie(0)->sync_with_stdio(0);

    int N; cin >> N;

    if(N % 3 == 0) cout << "Lalic\n";
    if(N % 3 == 1) cout << "Lalic\n";
    if(N % 3 == 2) cout << "Enzo\n";
}
